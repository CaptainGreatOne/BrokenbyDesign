{
  "name": "web-gateway",
  "version": "1.0.0",
  "description": "HTTP REST gateway that proxies to gRPC Order API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "@grpc/grpc-js": "^1.10.0",
    "@grpc/proto-loader": "^0.7.0",
    "redis": "^4.6.0",
    "uuid": "^9.0.0",
    "prom-client": "^15.1.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
